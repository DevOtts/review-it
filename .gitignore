# fable-it run artifacts (generated per run — never commit)
FABLE-IT-REPORT.md
SKILL-PUBLISH-REPORT.md
.fable-it-reports/
.taskstate/
*.credentials
.full.credentials

# review-it run artifacts (generated per run in consumer repos)
.review-it/

# Environment / secrets
.env
.env.*

# OS / editor cruft
.DS_Store
